services:
  app:
    build:
      context: .
      args:
        - MONGODB_URI=${MONGODB_URI}
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=${MONGODB_URI}
      - ENTRY_TIMEOUT_HOURS=${ENTRY_TIMEOUT_HOURS}
    restart: unless-stopped
